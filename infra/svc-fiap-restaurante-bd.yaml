apiVersion: v1
kind: Service
metadata:
  name: svc-db
  labels:
    app: mongo
spec:
  type: NodePort
  ports:
    - port: 27017
      protocol: TCP
      targetPort: 27017
  selector:
    app: restaurante-db
    
